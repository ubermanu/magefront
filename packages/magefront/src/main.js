export { magefront } from './magefront'

/**
 * Auxiliary function for defining the configuration object.
 *
 * @param {import('types').Config} options
 * @returns {import('types').Config}
 */
export function defineConfig(options) {
  return options
}
