{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "npmClient": "pnpm",
  "version": "independent",
  "ignoreChanges": [
    ".github"
  ],
  "packages": [
    "plugins/*",
    "presets/*",
    "."
  ]
}
